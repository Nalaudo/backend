Importar el archivo Backend.postman_collection.json dentro de postman para realizar metodos put y delete.
Crear archivo .env en la ruta base que contenga lo siguiente:

#INSTANCE=fs
#INSTANCE=mem
#INSTANCE=fireb
#INSTANCE=mongo

Descomentar la que quieras usar:
fs = filesystem
mem = memoria
fireb = firebase
mongo = mongoDB
